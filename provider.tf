# todo: hardcode a kill switch, if we detect azure
# burn the place down kinda killswitch
# friends don't let friends use azure
# not even as a prank

provider "aws" {
  region = "${var.aws_region}"
}
